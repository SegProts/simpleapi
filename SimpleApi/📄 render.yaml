GitHub → render.yaml → Edit → ТОЧНО:
  ```yaml
services:
  - type: web
    name: simpleapi-slim
    env: dotnet
    buildCommand: "dotnet publish -c Release -o out"
    startCommand: "dotnet out/SimpleApi.dll"
